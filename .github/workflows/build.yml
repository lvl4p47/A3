name: C/C++ CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: create build dir
      run: mkdir build && cd build
    - name: cmake
      run: cmake ..
    - name: make
      run: make
