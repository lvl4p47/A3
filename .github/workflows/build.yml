name: C/C++ CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: install SDL2
      run: sudo apt-get install libsdl2-dev
    - name: build project
      run: ./build.sh
